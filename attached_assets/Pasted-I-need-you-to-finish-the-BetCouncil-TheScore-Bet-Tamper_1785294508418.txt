I need you to finish the BetCouncil TheScore Bet Tampermonkey harvester. Real progress has already been made across several sessions — please pick up from here, don't restart from scratch.

Confirmed working, do not redo:

Correct GraphQL endpoint: https://sportsbook.us-default.thescore.bet/graphql
A hand-built, non-persisted query (not the stale persisted-query hash) that passes schema validation cleanly — confirmed via a real test call returning a genuine data key
The real, root blocker: auth. credentials: "include" alone never works because theScore's JWT lives in localStorage under key __SBWEB____AUTH_TOKENS__ (Zustand persist format: { state: { bearerToken: "..." } }), not in a cookie
A header-injection function was built that reads this token and sends it as Authorization: Bearer <token>, plus x-dma/x-device from sessionStorage key __SBWEB____VIEWER_DATA__

Where it's currently stuck: even with that real header-injection fix live-tested in the actual browser, it's still returning the same "You do not have permission to perform this action" / UNAUTHORIZED error, with data: {"competitionSection": null}. So either:

The token read from localStorage is coming back empty/null on this account, or
There's a genuine account/entitlement restriction (state licensing, geo-verification) blocking this specific resolver regardless of a valid token, or
Something else about the real header set is still incomplete

What I need:

Confirm empirically whether localStorage.getItem('__SBWEB____AUTH_TOKENS__') actually returns a real, non-null bearer token on this account, in this browser, right now
If it does, compare the complete real request headers from an actual successful in-browser GraphQL call (not just Authorization) against what the script sends — there may be another required header
If it doesn't, investigate why — account/session state, or whether this endpoint requires something the free/current account tier or state doesn't have access to

Hard constraint: this gets 1-2 more sessions, then we're done. If it's not fully working after that, we drop theScore entirely — remove the script, the workflow, and any related code from the codebase rather than leave it half-broken. Please report back with real, verified evidence either way (actual status codes, actual response bodies) — not a claim.