need help finishing a Tampermonkey userscript that harvests live game data (scores, lineups) from theScore Bet (sportsbook.thescore.bet) via their internal GraphQL API. Real progress has been made — I need you to pick up from a specific, well-documented blocker, not start over.

What's already confirmed working:

The correct target file and Gist push mechanism
The correct current persisted-query hash for operation CompetitionPageSectionLinesTabNode: 4fcab2e9b286b7b14db66c66280a38bceab9effed830e3a805e833d7ce8cac0b
Disabling a set of optional/cosmetic feature flags (includeRecommendedProps, isBrandingImageEnabled, etc.) fixed 35 of 36 real GraphQL errors

The current, specific blocker:
One error remains, on every sport: "Cannot query field \"abbreviation\" on type \"Team\"." — a GraphQL schema validation error, not a runtime error. This means the field is baked into the query document itself (not gated by any variable we control), so GraphQL rejects the whole request before returning any data at all — no partial success is possible for this class of error.

What I need you to actually do:

Confirm in a real browser (DevTools → Network → filter graphql) that theScore's own live site currently works fine when browsing game lines
Find whether their real, current request for this same data uses a different hash or operation name than the one above — capture the actual live variables and extensions.persistedQuery.sha256Hash from a real successful request, don't assume
If it's genuinely the same hash+operation, investigate whether their frontend passes something we're missing (different pageType, an additional required variable, etc.) that avoids triggering the broken field
Before telling me it works: show me the real HTTP status code and real response body from an actual test call — not a claim.

Here's the current full script for reference: [paste the script]

That prompt gives it the real findings so it doesn't repeat work, and builds in the same "show me real proof" standard I've been holding to throughout.