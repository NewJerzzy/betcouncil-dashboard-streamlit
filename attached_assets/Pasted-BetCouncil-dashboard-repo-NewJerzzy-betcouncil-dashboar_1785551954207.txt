BetCouncil dashboard — repo NewJerzzy/betcouncil-dashboard-streamlit,
Gist ID 7e52e1c2c2054847c7c4663a157386c5.

TASK: Find the real backend API for two sites using Playwright network
capture, since both only return static HTML from a plain HTTP request —
their real data loads via client-side JS after the page renders, which
a sandboxed HTTP-only environment can't see or execute.

SITE 1 — EVBets (evbets.app)
Target pages: https://evbets.app/value-bets/baseball-mlb
              https://evbets.app/prop-bets/baseball-mlb
Confirmed so far (via plain HTTP fetch): page returns real 200 HTML
(~96KB, Astro framework), but no embedded JSON, no matching data-table
CSS classes, and the JS bundle referenced in the HTML uses a
build-specific content hash that goes stale between fetches (i.e. you
can't hardcode the JS bundle URL — always re-derive it from a fresh
page load). Full sport-slug map already known:
  MLB: baseball-mlb, NBA: basketball-nba, NFL: american-football-nfl,
  NHL: hockey-nhl, UFC: mma-mixed-martial-arts, SOCCER: soccer-epl,
  WNBA: basketball-wnba

SITE 2 — VSIN (vsin.com)
Target pages: https://vsin.com/betting-splits/
              https://vsin.com/mlb/
Confirmed so far: real pages exist (WordPress/Newspaper theme), but the
actual bet%/handle% split numbers load via a JS call to
vsin.com/wp-admin/admin-ajax.php — need the real `action` parameter and
any nonce/auth token that call uses.

WHAT TO DO FOR EACH SITE
1. Launch a real headless browser (Playwright), open the target page,
   wait for full network idle (not just DOMContentLoaded -- the data
   loads asynchronously after initial render).
2. Capture every XHR/fetch network request the page makes after load.
   Look specifically for requests returning JSON with real
   odds/EV/bet-split data (not analytics/ads/tracking calls).
3. For each real data endpoint found, record: full URL, HTTP method,
   all request headers (especially any auth token, API key, or nonce),
   and a sample of the actual JSON response.
4. Test that endpoint again with a PLAIN requests.get()/post() call
   (no browser) using the exact headers captured, to confirm whether
   it's callable statically once you have the right headers/token, or
   whether it needs a fresh token every time (e.g. a nonce baked into
   the page HTML that changes per-load -- if so, note that the scraper
   will need to fetch the page first to extract the current nonce
   before calling the data endpoint, same as some other scrapers
   already do in this repo for CSRF-token-gated sites).

WHAT TO BUILD ONCE THE REAL ENDPOINTS ARE CONFIRMED
Two scripts, following the established pattern already used throughout
scripts/ in this repo (see scripts/bettingpros_refresh.py or
scripts/covers_consensus_refresh.py for the exact shape: log(), a
_rate_limit_ok() check against api.github.com/rate_limit before
pushing, push_files() with 403/429/409 retry+backoff, GITHUB_TOKEN from
env, main() returns 0/1, sys.exit(main())):

  scripts/evbets_refresh.py
    -> push ONE combined file: betcouncil_evbets_combined.json
       (NOT per-sport/per-type files -- the Gist is at a 300-file hard
       cap, confirmed repeatedly; any push creating a brand-new
       filename can silently fail. If you must create ANY new file,
       check current file count first via a GET on the gist and delete
       something confirmed-dead if needed, or ask before assuming
       there's room.)
    -> shape: {"captured_at": iso, "by_sport": {"MLB": {"value_bets":
       [...], "prop_bets": [...]}, "NBA": {...}, ...}}
    -> the real consumer already exists: app_core.py's "EVBets +EV
       signal overlay" (~line 16198) reads
       st.session_state["evbets_ev_picks"] and
       st.session_state["evbets_prop_picks"] as flat lists across all
       sports -- so also update fetch_evbets_* wiring in
       app_core.py's harvester loop (search for "_pf_" functions
       near "_pf_bettingpros" for the exact pattern to copy) to read
       from betcouncil_evbets_combined.json and flatten by_sport into
       those two session_state lists.

  scripts/vsin_splits_refresh.py
    -> push to a repurposed slot or new combined file, same 300-cap
       caution as above
    -> shape: whatever the real admin-ajax.php response gives you,
       normalized to at least {matchup, home_pct, away_pct} per game
       if it's a bet-percentage split (mirrors the existing SportsInsights
       wiring in Game Lines -- fetch_sportsinsights_from_gist in
       fetchers.py -- as the shape to match if this data is
       conceptually the same kind of public-betting-split signal)

VERIFICATION REQUIRED BEFORE CALLING EITHER DONE
- Trigger the real script via GH Actions workflow_dispatch (not just a
  local test) and confirm real, non-empty, correctly-shaped data lands
  in the Gist
- Syntax-check with python -m py_compile
- Re-download the pushed file from the Gist afterward and confirm it
  parses and contains real values, not empty placeholders
- If a nonce/token needs refreshing per-run, confirm the script does
  that automatically rather than needing a hardcoded value that will
  go stale

Report back the real endpoint URLs and required headers/auth for each
site once found, even if you don't finish full production wiring --
that alone unblocks the rest.